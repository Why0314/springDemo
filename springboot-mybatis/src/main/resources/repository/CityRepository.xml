<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.opay.org.spring.springboot.repository.CityRepository">
	<resultMap id="BaseResultMap" type="com.opay.org.spring.springboot.entity.City">
		<result column="id" property="id" />
		<result column="province_id" property="provinceId" />
		<result column="city_name" property="cityName" />
		<result column="description" property="description" />
	</resultMap>

	<sql id="Base_Column_List">
		id, province_id, city_name, description
	</sql>

	<select id="findByName" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		<include refid="Base_Column_List" />
		from city
		where city_name = #{cityName}
	</select>
    
    <select id="findListByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List" />
        from city
        where city_name = #{cityName}
    </select>
    
    <insert id="addCity" parameterType="com.opay.org.spring.springboot.entity.City" useGeneratedKeys="true" keyProperty="id">
        insert into city (province_id, city_name, description)
        values (#{city.provinceId}, #{city.cityName}, #{city.description})
    </insert>
    
    <insert id="addCityBatch" parameterType="java.util.List">
        insert into city (province_id, city_name, description) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.provinceId}, #{item.cityName}, #{item.description})
        </foreach>
    </insert>

    <update id="updateDataById" parameterType="com.opay.org.spring.springboot.entity.City">
        update city
        set province_id = #{city.provinceId},
            city_name   = #{city.cityName},
            description = #{city.description}
        where id = #{city.id}
    </update>
    
    <update id="updateByName" parameterType="com.opay.org.spring.springboot.entity.City">
        update city
        set province_id = #{city.provinceId},
            description = #{city.description}
        where city_name = #{city.cityName}
    </update>
    
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from city where id = #{id}
    </delete>
    
    <delete id="deleteByName" parameterType="java.lang.String">
        delete from city where city_name = #{cityName}
    </delete>
</mapper>
